# Finance---Dashboard---Backend
Clone the repository:

Bash
git clone https://github.com/ShannuF1/finance-backend.git

Navigate to the folder:

Bash
cd finance-backend

Update the database:

Bash
dotnet ef database update

Run the application:

Bash
dotnet run

Access Swagger UI: Open https://localhost:5001/swagger to test the API endpoints.


